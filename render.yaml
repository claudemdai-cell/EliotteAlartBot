services:
  - type: worker
    name: elliott-alert-bot
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python bot/scanner.py
    envVars:
      - key: TELEGRAM_TOKEN
        sync: false
      - key: TELEGRAM_CHAT_ID
        sync: false
      - key: WEBHOOK_SECRET
        sync: false
